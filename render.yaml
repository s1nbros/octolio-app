services:
  - type: web
    name: octolio-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
