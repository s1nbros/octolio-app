services:
  - type: web
    name: octolio-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install --omit=dev
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
